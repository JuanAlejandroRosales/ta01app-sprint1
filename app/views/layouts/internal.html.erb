<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Ta01app Sprint1" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Ta01app Sprint1" %>">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track' => 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => 'reload' %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <header>
      <%= render 'layouts/navigation' %>
    </header>
    <main role="main">
      
      <div class="container">
        <br>
        <div class="row">
          <div class="col-lg-9">
            <%= render 'layouts/messages' %>
               <div class="page-header">
                <h1><%= yield :header_title %> <small><%= yield :header_sub_title %></small></h1>
             </div>
            <%= yield %>
          </div>
        <div class="col-lg-3">
          <%= render 'layouts/sidebar' %>
        </div><!--/span-->
      </div><!--/row-->
      <div class="row">
        <div class="col-lg-10">
          <footer>
            <center>
              <p>&copy; Juan Alejandro Rosales Coronel 2017 - Todos los Derechos Reservados</p>
            </center>
          </footer>
        </div>
      </div><!--/row-->
    </div> <!-- /container -->
    </main>
  </body>
</html

